/**
 * GymOS v9 — Recovery Presets
 */

export const CARDIO_BY_GROUP = {
  upperHeavy: 12,
  upperHypertrophy: 14,
  upperPump: 12,
  lowerHeavy: 10,
  lowerAthletic: 12,
  recovery: 25,
};

export const DEFAULT_SLEEP_TARGET = '7.5–8.5 h';
export const DELOAD_SLEEP_TARGET = '8.0–9.0 h';

export const SAUNA_PRESETS = {
  normal: '1–2× weekly • 8–12 min',
  refeed: '1× weekly • 8–10 min',
  deload: '0–1× weekly • brief only',
};

export const STEAM_PRESETS = {
  normal: 'Optional after upper / mobility days',
  deload: 'Optional 1× • 6–8 min',
};
